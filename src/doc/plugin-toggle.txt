wp-plugin-deactivate(1) -- Activate/Deactivate an installed plugin.
====

## SYNOPSIS

`wp plugin toggle` <plugin> [--network]

## OPTIONS

* `<plugin>`:

	The plugin to toggle.

* `--network`:

	If set, the plugin will be toggled for the entire multisite network.
